{
  "mcpServers": {
    "flightphp-skeleton": {
      "command": "php",
      "args": ["mcp-server.php"],
      "cwd": "f:\\projetos\\mcp-skeleton-server",
      "env": {
        "PATH": "${env:PATH}"
      }
    }
  }
}
